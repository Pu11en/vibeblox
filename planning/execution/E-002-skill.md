# E-002 — Skill: the loop as a harness plugin

**Status:** complete
**Depends on:** E-001

## Outcome

`~/.agents/skills/play2build/SKILL.md` — any session can invoke the loop
(Drew-driven interactive, or agent-driven one-shot with `--auto-*`). The
"plugin that the harness uses" from P-001.

## Next eligible

E-003 (deploy web ideas) — deferred until web-app ideas appear.
