---
title: library
created: 2026-08-01
updated: 2026-08-01
type: concept
tags: [library]
sources: [generated from data/repos.sqlite3]
---

# library

Reusable code collections for developers.

**36 repos**

- **1146345502/aural-oss** - shaders, metal rendered views, competitors typically show a blank box where those should be. It runs an async capture pipeline that pauses itself during touches
- **1jehuang/mermaid-rs-renderer** - or iMessage. Instead of opening a browser, you just text your server, "Hey, did I pay that bill or summarize this PDF?" Because it stores everything in local fi
- **Aeastr/Conditionals** - Using the Windows Task Scheduler API, Phantom Task lets you create tasks that run in particular user sessions, list all active sessions on your machine, and exe
- **AlexsJones/llmfit** - No magic is a collection of single-file, dependency-free Python implementations of AI and ML algorithms. Every script trains a model from scratch and performs i
- **BlWasp/PhantomTask** - If you've ever wanted to peek under Bitcoin's hood and actually understand how it works, Python Bitcoin utils is your toolkit. This Python library is simple, le
- **GeneticistHere/ggplot2-20-journal-cases** - The output includes renders, reports, manifests, and hashes. Rebuilding a volcano plot or a Circos diagram from a paper usually starts with hunting for the auth
- **Lulzx/tinypdf** - GitHub never sleeps and today's trending list is absolutely stacked. If you love discovering open source projects before everyone else, smash that like button, 
- **Lulzx/zpdf** - tweets. Then it gets smart. Smog invokes clawed code to analyze each tweet, categorize it, and add useful context, not just raw links. ZPDF is a fast PDF text e
- **MagicCube/helixent** - the source document and draws a bounding box around the exact text on the page. Most terminal spinner libraries for Rust are bloated or require complex runtime 
- **Michael-A-Kuykendall/shimmy** - everything from streaming messages to state updates and UI actions. It's lightweight, flexible, and transport agnostic, meaning it works seamlessly over websock
- **MoonshotAI/MoonEP** - invent product facts. One file, no dependencies, so any model can run it. Mooney P is Moonshot AI's expert parallel communication library for keeping mixture of
- **Neighbor-Z/SwiftMTP** - with a microscopic memory footprint directly on the edge. No bouncing payloads back to a heavy AWS backend just to crop a photo. Prep images for your AI vision 
- **Sharp-ML/SHARP-ML** - headphones, and mics, and audio priority bar does the rest, switching to the highest priority device as soon as it connects. It's got separate modes for speaker
- **Smaug6739/Alexandrie** - everything organized. Slink lets you share images securely without relying on third party services. It's perfect for artists showcasing work, developers sharing
- **TanStack/charts** - policy checks with AWS sessions scoped through STS when possible. TanStack Charts is a typed visualization grammar that keeps your chart definition independent 
- **ahmadawais/chartli** - generators that just morph pixels, this is an open-source generative frame model designed specifically for spatial intelligence. Give it a single photo or text 
- **alexzhang13/rlm** - browser. It extracts the thumbnails on the fly using canvas, so you don't need a backend server or pre-generated sprite sheets. Perilane is a peer-to-peer file 
- **alibaba/Logics-Parsing** - consistency, and semantic coherence across frames. With these designs, a 1.3b parameter model can be fine-tuned for long video generation in just 32 GPU days. M
- **awrped/ferrite-json** - model that beat Minecraft. Built by Nicholas Hansen, this repo ports the groundbreaking Dreamer 4 architecture from Jax to PyTorch. Dreamer 4 is famous for bein
- **btahir/react-kino** - generate multiple takes per clip non-destructively, use context-aware gap fill, and regenerate specific sections of a video without leaving the timeline. Tired 
- **cordum-io/cordum** - test-driven development, automated hooks that lint and type check every file the AI writes. If you want to stop fighting with context windows, and start shippin
- **ctxrs/ctx** - tab. Charts rasterize to PNG when you ask. CTX is a Rust CLI that searches the coding agent history already stored on your machine, instead of giving every new 
- **dabit3/openform** - Every week, GitHub explodes with new open-source projects, and most of them completely fly under the radar. So, I went through GitHub trending and handpicked 30
- **dicroce/wyrm_math** - counter rejecting whichever right would clobber the other. Wormath is a symbolic algebra engine built so illegal moves can't happen rather than getting rejected
- **drclcomputers/GoSheet** - what matters. Subscribe to any RSS or Atom feed and organize everything with multi-level folders and tags. See something important? Hit read later or save exter
- **eihabkhan/reddojs** - built in Go. It's lightweight and perfect for handling thousands of connections with minimal resources. Bind 10,000 plus ports effortlessly, all with a single c
- **gonzalezreal/textual** - clusters. Get a dashboard view of all your nodes showing CPU, memory, and disk usage at a glance. Manage VMs and Lexe containers effortlessly. List them, start,
- **gsnoopy/react-old-icons** - you control the AI, whether that's Gemini with your own API key or local models like Olama. You even get distraction highlights, time-lapse replays of your day,
- **kasznar/geometric-constraint-solver** - queryable SQLite database, and optimize tags using AI assisted reasoning with human oversight. Diaposics is a portable electronic instrument and MIDI controller
- **kylehowells/swift-justhtml** - that writes novels, books, and short stories all on its own. Powered by Google's Gemini 3 Flash, this agent doesn't just type, it thinks deeply, plans plots, an
- **libpdf-js/core** - map you've been missing. Maharaga is an AI trading agent that monitors social sentiment from Stock Twits and Reddit, then executes trades through Alpaca for sto
- **moabukar/miniblue** - loop of autonomous agents with none of the orchestration bloat. Raw minimal building blocks for custom agent loops, LLM tool execution, and CLI skills at maximu
- **reinesana/MeowCV** - your repo and strips it down to just the essentials, function signatures, and dependencies. It reduces your token usage by 95%. Now you can fit a 100,000line pr
- **soufianekhiat/ImAnim** - can understand instantly. Orion store is a progressive Android application that acts as a fully functional app store without requiring a dedicated backend serve
- **voidhashcom/effect-query** - optimizes when to run jobs, day, night, or both. Basically, it's like hiring a dev assistant who literally never sleeps. Here's one for the TypeScript users, Ef
- **z-libs/Zen-C** - Meet Ngram, Deep Seek's brand new architecture that finally separates thinking from remembering. Right now, AI models waste huge amounts of compute just to reca

## Related

[[tool]] [[self-hosted]] [[ai]] [[web]]